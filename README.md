# Moon Rover Portfolio

Dev scaffold for the Moon Rover Three.js portfolio component.

Install and run:

```bash
npm install
npm run dev
```

Open the printed local URL.

Notes:
- Tailwind classes are enabled using the Play CDN in `index.html`.
- The project uses Vite and Three.js.
